{
  "name": "strava-mcp",
  "version": "0.2.0",
  "description": "MCP server serving a Strava bulk-export archive (activities, streams, profile, stats, routes, goals)",
  "type": "module",
  "bin": {
    "strava-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "prepare": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
